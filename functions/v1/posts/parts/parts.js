const functions = require('firebase-functions');
const express = require("express");
const app = express();

app.post("/heart", (req, res, next) => {
    //
})

app.post("/like", (req, res, next) => {

})

app.delete("/like", (req, res, next) => {

})

module.exports = app;
